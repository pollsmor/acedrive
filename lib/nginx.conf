# On cloud instance: $ sudo cp lib/nginx.conf /etc/nginx/nginx.conf
user www-data;

events {}

http {
    server {
        server_name kevinli8.cse356.compas.cs.stonybrook.edu;
        proxy_http_version 1.1;

        location / {
            proxy_pass http://localhost:3000;
        }
    }
}